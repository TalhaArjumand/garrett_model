# Manipulation Range Spec

## Purpose

This spec records the first source-derived doctrine for `manipulation range`
inside Garrett's universal-model framework.

It exists to capture the branch structure now supported by the reviewed
transcript without pretending that the final mechanics are already solved.

## Current Doctrinal Status

`manipulation range` is now directly present as a named universal-model branch.

It is still only partially defined.

## Direct Source-Derived Skeleton

From the reviewed transcript:

- manipulation ranges are also described as order-pairing ranges
- a high or low or range low can be the key level
- the opposing side of the range or swing high can be the targets

This is the strongest source-derived structure currently available.

## Current Reading

At the current reviewed level, manipulation range provides:

- a key-level object family
- a target-side object family

Current skeleton:

- key-level side:
  - high
  - low
  - range low
- target side:
  - opposing side of the range
  - swing high

## Relationship To The Universal Model

At the current repo doctrine level, Garrett's universal model now includes:

- `internal to external`
- `external to internal`
- `manipulation range`

So manipulation range should be treated as a third framework branch, not
merely as a small subtype of `IRL` or `ERL`.

## What Is Locked

Locked from the current reviewed transcript:

- `manipulation range` is part of the universal-model framework
- it is also described as an order-pairing range
- a high / low or range low can act as the key level
- the opposing side of the range or swing high can act as the target

## What Is Not Yet Locked

Not yet locked:

1. the exact operational definition of a manipulation range
2. the exact bullish / bearish mirror wording
3. whether the listed key-level objects are exhaustive
4. whether the listed target objects are exhaustive
5. how manipulation range interacts with `IRL`, `ERL`, and later SMT logic
6. a machine-ready detector

## Important Boundary

Do not code manipulation range yet as if it were fully defined.

At the current stage, the repo only has:

- branch name
- key-level object family
- target-side object family

It does not yet have final selection mechanics or final direction rules.

## Implementation Status

Implementation status: doctrine only.
